{
  "name": "firmo",
  "private": true,
  "description": "Firmo — single-source, lazy-loading /firmo <tool> skill set for Codex and Claude Code",
  "scripts": {
    "build": "node build.mjs",
    "format": "oxfmt",
    "agent:check": "oxfmt --check"
  },
  "devDependencies": {
    "oxfmt": "^0.56.0"
  },
  "packageManager": "pnpm@11.9.0"
}
